questions = {
    "(0) 次のプログラムに使われているローカル変数とグローバル変数を答えなさい。": 2,
    "(1) 標準名前空間の内容を調べて、今まで講義で使ったことがないPythonの標準関数の使い方を調べて、説明しなさい。": 1,
    "(2) 関数`make_globals_list()`は、グローバル名前空間にある変数名をリストとして返す。その関数を定義しなさい。": 2,
    "(3) Python `in` オペレータで、リストの中に値が存在するかどうか確認ができる。": 2,
    "(4) 関数`list_intersection(l1,l2)`は2つのリストを引数として受け入れて、両方のリストにある値のリストを返す。その関数を定義しなさい。": 2,
    "(5) 関数`check_namespace_duplicate()`は、ローカル名前空間とグローバル名前空間両方に同じ名前が存在する場合、それのリストを作成して返す。": 2,
    "ファイルを `open()` で開いた後、`close()` で閉じることが普通である。もし `close()` しなかった場合、どのような問題が生じるか。生じうる例を 2 つ上げなさい。": 1,
    "(1) `cat -n`と同様の動作をする関数 `cat_n()` を定義し、この関数の引数として `'zen-of-python.txt'` を与えた実行結果を示しなさい。": 2,
    "(2) `fgrep`と同様の動作をする関数 fgrep() を定義しなさい。": 3,
    "(3) `head`と同様の動作をする関数 head() を定義しなさい。": 3,
    "(1) 関数 `make_sum_number(l1)`を定義しなさい": 2,
    "(2) 作成したスクリプトファイル `question3.py` の内容を、以下に書き込みなさい": 1,
    "(3) 端末プログラムでlsコマンドやdirコマンドでスクリプトの存在を確認する": 1,
    "(4) スクリプトの実行結果を、以下に書き込みなさい": 1,
    "(1) 二次元リスト`a`の横のインデックス`1`と縦のインデックス`3`を参照する時、Pythonでの書き方:": 1,
    "(2) 繰り返し構造の中に繰り返し構造を入れることの呼び方。": 1,
    "(3) CSVの意味:（英語と和訳）": 1,
    "(4) CSV形式のファイルからデータを読み込むための関数を書きなさい。": 1,
    "(5) CSV形式のファイルを書き込むための関数`csv.writer()`の引数`delimiter`の意味を説明しなさい:": 1,
    "(1) `wc`と同様の動作をする関数 wc() を定義しなさい。": 3,
    "(2)`diff`のような操作をする`diff(f1, f2)`関数を定義しなさい。": 4,
    "(5)　上の課題で定義された`diff()`関数を利用して、`diff.py`スクリプトを作成しなさい。スクリプトは、２つのファイル名を引数として受け入れて、`diff()`関数のような出力を表示する。": 1
}
questions_number = {
    "(0) 次のプログラムに使われているローカル変数とグローバル変数を答えなさい。": "1",
    "(1) 標準名前空間の内容を調べて、今まで講義で使ったことがないPythonの標準関数の使い方を調べて、説明しなさい。": "2",
    "(2) 関数`make_globals_list()`は、グローバル名前空間にある変数名をリストとして返す。その関数を定義しなさい。": "3",
    "(3) Python `in` オペレータで、リストの中に値が存在するかどうか確認ができる。": "4",
    "(4) 関数`list_intersection(l1,l2)`は2つのリストを引数として受け入れて、両方のリストにある値のリストを返す。その関数を定義しなさい。": "5",
    "(5) 関数`check_namespace_duplicate()`は、ローカル名前空間とグローバル名前空間両方に同じ名前が存在する場合、それのリストを作成して返す。": "6",
    "ファイルを `open()` で開いた後、`close()` で閉じることが普通である。もし `close()` しなかった場合、どのような問題が生じるか。生じうる例を 2 つ上げなさい。": "7",
    "(1) `cat -n`と同様の動作をする関数 `cat_n()` を定義し、この関数の引数として `'zen-of-python.txt'` を与えた実行結果を示しなさい。": "8",
    "(2) `fgrep`と同様の動作をする関数 fgrep() を定義しなさい。": "9",
    "(3) `head`と同様の動作をする関数 head() を定義しなさい。": "10",
    "(1) 関数 `make_sum_number(l1)`を定義しなさい": "11",
    "(2) 作成したスクリプトファイル `question3.py` の内容を、以下に書き込みなさい": "12",
    "(3) 端末プログラムでlsコマンドやdirコマンドでスクリプトの存在を確認する": "13",
    "(4) スクリプトの実行結果を、以下に書き込みなさい": "14",
    "(1) 二次元リスト`a`の横のインデックス`1`と縦のインデックス`3`を参照する時、Pythonでの書き方:": "15",
    "(2) 繰り返し構造の中に繰り返し構造を入れることの呼び方。": "16",
    "(3) CSVの意味:（英語と和訳）": "17",
    "(4) CSV形式のファイルからデータを読み込むための関数を書きなさい。": "18",
    "(5) CSV形式のファイルを書き込むための関数`csv.writer()`の引数`delimiter`の意味を説明しなさい:": "19",
    "(1) `wc`と同様の動作をする関数 wc() を定義しなさい。": "20",
    "(2)`diff`のような操作をする`diff(f1, f2)`関数を定義しなさい。": "21",
    "(5)　上の課題で定義された`diff()`関数を利用して、`diff.py`スクリプトを作成しなさい。スクリプトは、２つのファイル名を引数として受け入れて、`diff()`関数のような出力を表示する。": "22"
}
questions_name_to_number = {
    "1": "1-0",
    "2": "1-1",
    "3": "1-2",
    "4": "1-3",
    "5": "1-4",
    "6": "1-5",
    "7": "2-1",
    "8": "2-2-1",
    "9": "2-2-2",
    "10": "2-2-3",
    "11": "3-1",
    "12": "3-2",
    "13": "3-3",
    "14": "3-4",
    "15": "4-1",
    "16": "4-2",
    "17": "4-3",
    "18": "4-4",
    "19": "4-5",
    "20": "5-1",
    "21": "5-2",
    "22": "5-3"
}
